## MODIFIED Requirements

### Requirement: Frontmatter defines a Python generator instead of Rust algorithm

The `.md` file's frontmatter SHALL contain a `generator` field (YAML block scalar) holding a Python script that acts as a reference solution. The frontmatter SHALL also contain a `params` object (YAML) specifying the random input parameter types and a `starter_code` field with the student template. No TOML file SHALL be used to store this data.

The generator script SHALL operate in one of two modes:

1. **Standard mode**: The script SHALL read random inputs from stdin (generated by Rust WASM based on `params`) and print the expected output to stdout.

2. **Factory mode**: The script SHALL generate its own valid inputs internally and print a single JSON object to stdout in the format `{"input": "<input_string>", "expected_output": "<expected_output_string>"}`. In this mode, the WASM-generated stdin input SHALL be ignored by the generator. The `params` field SHALL still be present in frontmatter to satisfy the orchestration layer (which uses it to determine how many times to invoke the generator), but the param values SHALL NOT affect the generator's output. A YAML comment SHALL be placed in the `params` block to indicate factory mode usage.

Factory mode SHALL be used when the challenge requires cross-parameter constraints that the WASM `ParamSpec` system does not support (e.g., prime numbers, coprimality requirements, relative value constraints between parameters).

#### Scenario: ChallengeView reads generator from frontmatter

- **WHEN** a challenge page loads
- **THEN** `ChallengeView` reads `frontmatter.generator`, `frontmatter.params`, and `frontmatter.starter_code` without loading any external file

#### Scenario: Missing generator field causes visible error

- **WHEN** a challenge page loads and `frontmatter.generator` is undefined or empty
- **THEN** the challenge page displays an error state indicating the generator is missing

#### Scenario: Factory mode generator outputs JSON with transformed input

- **WHEN** a generator script prints a JSON string `{"input": "61\n53\n17\n65", "expected_output": "3233 2753 2790"}` to stdout
- **THEN** the orchestration layer (dev Worker or pool generator) SHALL use the JSON `input` field as the student-facing input and the `expected_output` field as the expected output, discarding the original WASM-generated stdin

#### Scenario: Factory mode generator produces mathematically valid RSA parameters

- **WHEN** the RSA basic challenge generator runs in factory mode
- **THEN** the generated input SHALL contain prime p and q (in [10, 97]), a public exponent e coprime with phi=(p-1)(q-1), and plaintext m in [0, n-1], ensuring `pow(e, -1, phi)` SHALL NOT raise an error
